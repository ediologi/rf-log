# robot.yaml
version: 1

python:
  # Use the virtual environment Python
  executable: ./venv/bin/python

paths:
  # Test case and resource discovery
  - tests
  - resources

robot:
  # Load environment variables automatically
  variables:
    ENV: .env
  # Default command-line arguments for "Run Test"
  arguments:
    - -d
    - output
